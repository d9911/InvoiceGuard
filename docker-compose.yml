version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: invoice-guard-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7.0-alpine
    container_name: invoice-guard-redis
    ports:
      - "6379:6379"

volumes:
  mongo_data:
